# csolution / cbuild generated build artifacts
RTE/
out/
tmp/
*.cbuild-idx.yml
*.cbuild-pack.yml
*.cbuild-set.yml
*.cbuild-run.yml
__pycache__/
run.sh
md5.*



# models/, all_ops.cproject.yml and embedded_models_blob.S / embedded_models.{cpp,h}
# are checked in here so the project builds standalone (the upstream generators
# that produce them live in the ExecuTorch tree, not in this snapshot).
